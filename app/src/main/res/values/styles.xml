<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ToolbarColoredBackArrow" parent="AppTheme">
        <item name="android:textColorSecondary">@color/color_black</item>
    </style>

    <style name="ToolbarColoredBackArrow65" parent="AppTheme">
        <item name="android:textColorSecondary">@color/color_black_65</item>
    </style>

    <style name="ToolbarColoredBackArrowWhite" parent="AppTheme">
        <item name="android:textColorSecondary">@color/color_white</item>
    </style>

    <style name="MyDatePickerDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <!-- this is new -->
        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="android:datePickerStyle">@style/Theme.AppCompat.Light.Dialog</item>
    </style>

    <style name="MyDatePickerDialogThemes" parent="Theme.AppCompat.Light.Dialog">
        <!-- this is new -->
        <item name="android:datePickerStyle">@style/MyDatePickerStyle</item>
    </style>

    <style name="MyDatePickerStyle" parent="@android:style/Widget.Material.Light.DatePicker">
        <item name="android:headerBackground">@color/colorAccent</item>
    </style>

    <!-- Theme.AppCompat.Light is not a dialog theme -->

    <style name="TimePickerTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="MineCustomTabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">16dp</item>
        <item name="android:fontFamily">@font/proxima_nova_semibold</item>
        <item name="textAllCaps">false</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>
